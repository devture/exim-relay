apiVersion: v1
kind: Secret
metadata:
  name: {{ template "exim.fullname" . }}
  labels:
    app: {{ template "exim.name" . }}
    chart: {{ template "exim.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:  
  smtp-relay-password: {{ if .Values.smtp.relayPassword }}{{ .Values.smtp.relayPassword | b64enc }}{{ else }}""{{ end }}